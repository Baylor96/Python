array()
创建数组(无维度属性)
array()[:,np.newaxis]
给数组赋予维度
arange()
功能类似range()，默认整形
copy()
深拷贝
any()
数据是否包含有ture的值
linspace(start, stop, num=50, endpoint=True, retstep=False, dtype=None, axis=0)
start:返回样本数据开始点
stop:返回样本数据结束点
num:生成的样本数据量，默认为50
endpoint：True则包含stop；False则不包含stop
retstep：如果为True则结果会给出数据间隔
dtype：输出数组类型
axis：0(默认)或-1
np.set_printoptions(precision=x)
设置保留小数点后x个数
sum()
求和函数
product()
求积函数
average()
加权平均数
var()
方差
mean()
期望(平均值)
std()
标准差
transpose()
转置
flatten()
返回数组中每一个值
vstack()
上下合并数组
hstack()
左右合并数组
concatenate(:,:,axis=)
在axis轴上，合并多个数组
split(a,2,axis=)
在axis轴上，分割数组(必须等量分割)
array_split()
功能同split(可以不等量分割)
vsplit()
垂向分割数组(等量分割)
hsplit()
横向分割数组(等量分割)
min()，max()
最小值和最大值，同样可以指定axis参数　　
minimum(),maximum()　　
二元最小值与二元最大值　　
ptp()　　
最大值与最小值的差值　　
argmin()与argmax()　　
最小值下标和最大值下标　　
sort()　　
数组排序　　
argsort()　　
计算数组排序的下标　　
lexsort()　　
多列排序　　
partition()　　
快速计算前k位　　
argpatiton()　　
前k位下标　　
median()　　
中位数　　
percentile()　　
百分位数　　
searchsorted()　　
二分查找
clip(a,min,max)
滤波函数,将数组小于min替换成min，将数组中大于max替换成max
radnom.normal(0,1,n)
生成n个0-1符合正态分布的随机数
random.uniform(low,high,size)
从一个均匀分布[low,high)中随机采size个样